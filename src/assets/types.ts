export interface Price {
  title: string;
  firstDescription: string;
  secondDescription: string;
  price: string;
  img: string;
}

export interface Review {
  img: string;
  name: string;
  descr: string;
}
